{
  "name": "myproject",
  "version": "1.0.0",
  "scripts": {
    "build": "gulp build && gulp package && gulp edge:copyappx && powershell .\\build.ps1 && gulp clean",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-bom": "1.0.0",
    "gulp-rename": "1.2.2",
    "gulp-shell": "0.6.3",
    "manifoldjs": "0.7.6",
    "rimraf": "2.6.1"
  }
}
